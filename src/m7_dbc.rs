//! M7 draft DBC — re-exported from the shared `sigma-racer-sidearm` crate,
//! which is the single source of truth for the M7 CAN contract shared with the
//! M7 firmware and ECU.

pub use sigma_racer_sidearm::{dbc as m7_dbc, Dbc};
